requests>=2.31.0
curl_cffi>=0.7.0

# Optional: only needed if you use live transcription (Transcribe: ON)
faster-whisper>=1.0.0

# Optional: only needed for automatically clearing bot-check/verification pages.
# After installing, also run: playwright install chromium
playwright>=1.45.0
